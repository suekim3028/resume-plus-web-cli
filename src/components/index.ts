import { UI } from "@constants";
import { L } from "@web-core";
import TopBarContainer from "./TopBarContainer/TopBarContainer";

const Flex = L.FlexComponentGenerator(UI.COLORS);

export { Flex, TopBarContainer };
